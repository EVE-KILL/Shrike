# Private loopback origin used only by Nuxt SSR and renderer-owned routes.
# Browser requests always stay relative on /api, /auth, /images, and /ws.
NUXT_API_ORIGIN=http://127.0.0.1:4000

# Optional development-only upstream for same-origin /images requests.
NUXT_DEV_IMAGE_PROXY_ORIGIN=https://eve-kill.com

# Optional override for the public MCP base URL (the transport is at /mcp).
NUXT_PUBLIC_PUBLIC_MCP_URL=https://eve-kill.com/api
